extends layout
block contenido
    .contenido-principal
        h1 #{nombrePagina}    
        .contenedor-acciones
            //pre= vardump(proyecto.id)
            a(href= `/proyecto/editar/${proyecto.id}` class="boton") Editar Proyecto
            button#eliminar-proyecto(type="button" class="boton eliminar") Eliminar